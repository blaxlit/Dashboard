```markdown
# 🎮 Discord Server Analytics Dashboard

Dashboard สำหรับแสดงผลและวิเคราะห์ข้อมูลจำลองของกิจกรรมภายใน Discord Server พัฒนาด้วยภาษา Python โดยใช้ Streamlit และ Plotly

## 🚀 วิธีการรันโปรแกรม (How to run)
1. ติดตั้ง Library ที่จำเป็นทั้งหมด โดยพิมพ์คำสั่งใน Terminal:
   ```bash
   pip install -r requirements.txt

```

2. รันหน้า Dashboard ด้วยคำสั่ง:
```bash
streamlit run app.py

```


3. ระบบจะเปิดหน้าเว็บ Browser ขึ้นมาอัตโนมัติ (หรือสามารถเข้าผ่าน Local URL ที่แสดงใน Terminal ได้)

## 💻 อธิบายการทำงานของโค้ด (Code Explanation)

โปรเจกต์นี้ประกอบไปด้วยไฟล์หลักคือ `app.py` ซึ่งมีการทำงานดังนี้:

* **การโหลดข้อมูล:** ใช้ `pandas` (`pd.read_csv`) ในการอ่านไฟล์ข้อมูลจำลอง `discord_data.csv`
* **ส่วนแสดงผล (UI) & Interactive:** ใช้ `streamlit` ในการสร้างหน้าเว็บ โดยมี Sidebar พร้อม Dropdown ให้ผู้ใช้สามารถเลือกกรองข้อมูลตามห้อง (Channel) ได้ เมื่อผู้ใช้เปลี่ยนห้อง ข้อมูลในตาราง (`filtered_df`) จะถูกคำนวณใหม่แบบ Real-time
* **ส่วนการสร้างกราฟ (Data Visualization):** ใช้ `plotly.express` ในการสร้าง Interactive Graphs 3 รูปแบบ ได้แก่:
1. **Bar Chart (`fig_bar`):** แสดงอันดับผู้ใช้งานที่มีกิจกรรมสูงสุด (Top Users) โดยรวมค่าจากคอลัมน์ Value
2. **Pie Chart (`fig_pie`):** แสดงสัดส่วนประเภทของกิจกรรม (Text vs Voice)
3. **Line Chart (`fig_line`):** แสดงแนวโน้มของกิจกรรมที่เกิดขึ้นในแต่ละวัน (Daily Activity Trend) เพื่อดูภาพรวมความหนาแน่นของการใช้งาน
